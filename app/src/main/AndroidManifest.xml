<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.freelance.anantahairstudioadmin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/main_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/main_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnantaHairStudioAdmin"
        android:usesCleartextTraffic="true">
        <activity android:name=".gallery.AllPicsActivity"></activity>
        <activity android:name=".adminInfo.AdminInfoActivity" />
        <activity android:name=".addService.AllServicesActivity" />
        <activity android:name=".invoice.InvoiceActivity" />
        <activity android:name=".acceptedBooking.AcceptedBookingDetailsActivity" />
        <activity android:name=".allBooking.BookingDetailsActivity" />
        <activity android:name=".addService.AddServiceActivity" />
        <activity android:name=".acceptedBooking.AcceptedBookingActivity" />
        <activity android:name=".signup.SignUpActivity" />
        <activity android:name=".allBooking.AllBookingsActivity" />
        <activity android:name=".home.HomeActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/main_logo" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorPrimary" />

        <service
            android:name=".notification.FCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.freelance.anantahairstudioadmin.provider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>